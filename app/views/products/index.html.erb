<h1>List of Products</h1>


<% @products.each do |product| %>
<a href="<%= product_path(product) %>">
  <ul>
    <li>
      <%=  "#{product.name}" %>
    </li>
    <li>
      <%=  "#{product.category}" %>
    </li>
    <li>
      <%=  "#{product.quantity} " %>
    </li>
    <li>
      <%=  "#{product.price}" %>
    </li>
  </ul>
</a>
<% end %>
