<ul class="individual-order">
  <li><%= link_to "ORDER ##{individual_order.id}", order_path(individual_order) %></li>
  <li><%= individual_order.created_at.strftime("%Y AD / %b  %-d") %></li>
  <li><%= individual_order.total %></li>
</ul>
